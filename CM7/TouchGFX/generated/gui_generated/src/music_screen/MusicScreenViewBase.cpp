/*********************************************************************************/
/********** THIS FILE IS GENERATED BY TOUCHGFX DESIGNER, DO NOT MODIFY ***********/
/*********************************************************************************/
#include <gui_generated/music_screen/MusicScreenViewBase.hpp>
#include <touchgfx/Color.hpp>

MusicScreenViewBase::MusicScreenViewBase()
{
    /* ── Background: full 800×480, near-black ───────────────────────────── */
    __background.setPosition(0, 0, 800, 480);
    __background.setColor(touchgfx::Color::getColorFromRGB(18, 18, 18));
    add(__background);

    /* lblTitle and lblArtist are configured in MusicScreenView::setupScreen()
     * (after setWildcard() is called) to match the Screen1View pattern.   */

    /* ── Thumbnail: fullscreen PixelDataWidget (800x480 RGB888) ─────── *
     * Buffer is set to nullptr until the first JPEG arrives.            *
     * draw() is a no-op while buffer == nullptr, so no garbage appears. */
    imgThumbnail.setPosition(0, 0, 800, 480);
    imgThumbnail.setBitmapFormat(touchgfx::Bitmap::RGB888);
    imgThumbnail.setPixelData(nullptr);
    imgThumbnail.setAlpha(0);   /* invisible until setThumbnail() fires */
    add(imgThumbnail);
}

MusicScreenViewBase::~MusicScreenViewBase()
{
}

void MusicScreenViewBase::setupScreen()
{
}

void MusicScreenViewBase::tearDownScreen()
{
}
