<?xml version="1.0" encoding="utf-8"?>
<page>
  <title>Introduction</title>
  <keywords>
    <keyword>intervals</keyword>
    <keyword>states</keyword>
  </keywords>
  <body>
		<section>
			<requirement inverted="true">CoreFeatures.IntervalsAndStateMachines</requirement>
			<header>Overview</header>
			<text>
				<p>
					$PNAME does not support intervals or state machines, but Percepio Tracealyzer does.
				</p>
			</text>
		</section>
    <section>
			<requirement>CoreFeatures.IntervalsAndStateMachines</requirement>
      <header>Overview</header>
      <text>
		<p>
			$PNAME allows for defining custom data sets, based on events in the trace, which can be visualized and analyzed in several ways. 
			This allows you to analyze system-specific properties, i.e. the things that really matters for <i>your</i> system.
		</p>
		<p>
			There are two kinds of custom data sets: Intervals and State Machines.
		</p>
		<p>
			An <i>Interval</i> is used to measure or highlight the time between two specific events. 
			By adding an Interval definition, you create a data set containing all intervals (pairs of events) matching the definition. 			
		</p>

		<p>
			<i>Example:</i> Assuming a trace containing the user events "On" and "Off", you can define an interval starting at "On" and ending at "Off", 
			and thereby see all "On" -> "Off" sequences, independent of other events in between.
		</p>
		  
		<p>
			A <i>State Machine</i> is used to visualize any kind of state information logged in the trace.
			When defining a state machine, you specify the events corresponding to related state changes, and how to extract the state information from those events. 
			This results in a group of interval data sets, one for each observed state, representing the state machine.
		</p>
		  
		<p>
			<i>Example:</i> Assuming a trace containing the user events "On" and "Off", you can define a state machine "MyFSM" based on these events. Each occurrence of "On" or "Off" is then interpreted as the new state of "MyFSM".			
		</p>
		
		<p>
			$PNAME also provides a predefined list of intervals and states for objects that $PNAME is aware of.
			This way you can include, e.g., task scheduling states, and even TCP socket states (assuming you are using a TCP stack supported by $PNAME).
		</p>
		  	  
		<p>	        
			Intervals and states can be visualized and analyzed in several ways, as shown in the below example.
			In this example, the intervals are generated by defining a state machine based on user events, but all kinds of intervals can be shown in this way.
		</p>
		  
     	<p>
			 <A HREF="img/States in Vertical Trace.png" target="$IMAGETARGET">
				<img src="img/States in Vertical Trace.png" width="$IMAGEWIDTH" border="0" />
			</A>
		</p>
		
		<p>
			Intervals and states can be displayed in several $PNAME views, and you can also generate statistics.
		
			<UL class="list">
		
				<li>
					<b>Trace View</b><br/>
					Intervals/states are shown in the trace view as a separate "Interval" field, in parallel with the RTOS trace. This effectively provides a software logic analyzer.			
				</li>
		  
				<li>
					<b>Interval Timeline</b><br/>
					Intervals/states can also be displayed in a separate timeline using <i>Interval Timeline</i> view, as shown in the upper right view above.								
				</li>
		
				<li>
					<b>Interval Plot</b><br/>
					The duration of intervals can be plotted over time, as shown in the lower right view above. The X axis indicates the start time of the interval, and the Y axis shows the duration.
					This way, you can study the time between two events and how it changes over time.								
				</li>
		 
				<li>
					 <b>State Machine Graphs</b><br/>
					 A state machine can be viewed as a directed graph, showing all observed states and state transitions. 
					Just open the <A HREF="FsmGraph.html">State Machine Graph</A>. 
				</li>
		 
				<li>
					<b>Statistics</b><br/>		  
					You may generate a statistics report for intervals, including state intervals, where you can see the shortest, longest and average duration over the whole trace.
					Do this by opening the <A HREF="IntervalInstances.html">Intervals and State Machines</A> window and then right-click on the interval definition in the list.					
				</li>
			 </UL>
			
			 <b>Note:</b> You may use the <A HREF="Filter.html">Filter</A> view to select which intervals to display.
		 </p>
		
		<p>			
			Intervals and state machines are added by using <i>Views</i> -&gt; <i>Intervals and State Machines</i>, shown in the below screenshot.
			Here you see all your custom data sets and you also have three buttons for adding new definitions.
			Learn more about this dialog on the <A HREF="IntervalInstances.html">Intervals and State Machines tool</A> page. 
		</p>
		
		<p>
			 <A HREF="img/intervals_and_state_machines.png" target="$IMAGETARGET">
				<img src="img/intervals_and_state_machines.png" border="0" />
			</A>
		</p>
        <subsection>
          <header>Runnables</header>
          <text>
            <p>
							A runnable is a software component that runs within an actor, typically implemented as a single C function. The concept is mainly used in automotive software. In $PNAME, runnables are implemented as a special case of intervals, where the interval set and channels are bound to an actor. 
            </p>
            <p>
							When intervals are bound to an actor, their calculated metrics change from Length, Separation and Periodicity to Execution Time, Response Time, Wait Time, Separation and Periodicity.
							<ul>
                <li>
                  <b>Execution Time</b> is the time between the start and end point where the associated actor is also switched in by the scheduler.
                </li>
                <li>
                  <b>Response Time</b> is the time between the start and end point.
                </li>
                <li>
                  <b>Wait Time</b> is the time between the start and end point where the associated actor is NOT switched in by the scheduler.
                </li>
                <li>
                  <b>Separation</b> is the time between the end of the previous interval and the start of the current one. Identical to regular intervals.
                </li>
                <li>
                  <b>Periodicity</b> is the time between the start of the previous interval and the start of the current one. Identical to regular intervals.
                </li>
              </ul>
            </p>
            <p>
							The rendering in the main trace view will change from a single box per interval to fragmented style (very similar to actor instances) when an interval is not fully contained within a single fragment of the associated actor. In addition, the new <i>Actors with Intervals</i> view preset in the main trace view will create a layout where any bound interval set will be placed right next to their associated actor.
            </p>

            <p>
							The runnables support in $PNAME has been developed with the automotive use case in mind and has the following limitations.
            </p>

            <p>
              <ul>
                <li>
                  Each runnable must be bound to one specific actor.
                </li>
                <li>
                  Each actor can have 0 or 1 runnable active, they can never be nested. The runnables support is for top level activities within an actor, and can't be used for complete nested function tracing.
                </li>

              </ul>
            </p>
          </text>
        </subsection>
		</text>
			
    </section>
  </body>
</page>